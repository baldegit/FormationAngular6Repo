import { ContactSearchePipe } from './contact-searche.pipe';

describe('ContactSearchePipe', () => {
  it('create an instance', () => {
    const pipe = new ContactSearchePipe();
    expect(pipe).toBeTruthy();
  });
});
