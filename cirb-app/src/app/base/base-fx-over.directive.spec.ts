import { BaseFxOverDirective } from './base-fx-over.directive';

describe('BaseFxOverDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseFxOverDirective();
    expect(directive).toBeTruthy();
  });
});
