import { BaseFxDirective } from './base-fx.directive';

describe('BaseFxDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseFxDirective();
    expect(directive).toBeTruthy();
  });
});
